// 66권 표준 절 수(개역개정/KJV 계열 절 구분, 합계 31,102). index 0 = 창세기(bookNo 1).
//
// 왜 정적 표인가: 백엔드 진척 API(GET /users/me/progress)는 "전체 절수"만 주고 권별 절수는 주지 않는다.
// 밤하늘 은하는 권마다 크기가 다른 성단을 그려야 하고(시편은 크게, 오바댜는 작게),
// 카테고리(경전 타입)별 진행률도 분모가 있어야 계산되므로 이 표가 필요하다.
//
// ⚠️ 이 표의 용도는 "은하 형태의 가중치 + 카테고리별 분모"까지다.
//    화면에 크게 뜨는 진척 수치(1,108 / 31,102절)는 언제나 서버 응답(totalVerses)을 쓴다.
//    번역본에 따라 절 구분이 몇 절 다를 수 있으므로, 이 표로 낸 비율은 항상 1로 클램프한다.

export const BOOK_VERSE_COUNTS: readonly number[] = [
  1533,
  1213,
  859,
  1288,
  959, // 창세기~신명기
  658,
  618,
  85,
  810,
  695,
  816,
  719,
  942,
  822,
  280,
  406,
  167, // 여호수아~에스더
  1070,
  2461,
  915,
  222,
  117, // 욥기~아가
  1292,
  1364,
  154,
  1273,
  357, // 이사야~다니엘
  197,
  73,
  146,
  21,
  48,
  105,
  47,
  56,
  53,
  38,
  211,
  55, // 호세아~말라기
  1071,
  678,
  1151,
  879, // 마태복음~요한복음
  1007, // 사도행전
  433,
  437,
  257,
  149,
  155,
  104,
  95,
  89,
  47,
  113,
  83,
  46,
  25, // 로마서~빌레몬서
  303,
  108,
  105,
  61,
  105,
  13,
  14,
  25, // 히브리서~유다서
  404, // 요한계시록
];

/** bookNo(1~66) → 절 수. 범위를 벗어나면 0. */
export function verseCountOf(bookNo: number): number {
  return BOOK_VERSE_COUNTS[bookNo - 1] ?? 0;
}

/** 66권 절 수 합계(31,102) — 카테고리 비중 계산의 기준. */
export const TOTAL_VERSE_COUNT = BOOK_VERSE_COUNTS.reduce((sum, n) => sum + n, 0);
